<Window x:Class="MarantzDesktopControl.ProfileSetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Receiver Profile"
        Height="280"
        Width="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#1B2029">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="First-time setup" Foreground="#E5E7EB" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,10" />

        <StackPanel Grid.Row="1" Margin="0,0,0,8">
            <TextBlock Text="Profile Name" Foreground="#E5E7EB" />
            <TextBox x:Name="ProfileNameTextBox" Text="Living Room" />
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,8">
            <TextBlock Text="Receiver IP" Foreground="#E5E7EB" />
            <TextBox x:Name="IpAddressTextBox" Text="192.168.1.40" />
        </StackPanel>

        <TextBlock Grid.Row="3" x:Name="ValidationText" Foreground="#FCA5A5" TextWrapping="Wrap" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="90" Margin="0,0,8,0" Click="CancelButton_Click" />
            <Button Content="Save" Width="90" Click="SaveButton_Click" />
        </StackPanel>
    </Grid>
</Window>
